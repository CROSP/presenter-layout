<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <!--this is to give gradient effect -->
    <stroke
        android:width="1dp"
        android:color="@color/white"/>
    <solid android:color="@color/primary_green_color" />
    <!-- this is make corners of button rounded -->
    <corners
        android:bottomLeftRadius="@dimen/radius_base_background"
        android:bottomRightRadius="@dimen/radius_base_background"
        android:topLeftRadius="@dimen/radius_base_background"
        android:topRightRadius="@dimen/radius_base_background"/>

</shape>